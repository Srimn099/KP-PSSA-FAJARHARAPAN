<div class="box-info box-info-{{ $colors ?? 'primary' }}">
    <div class="box-info-icons">
        <i class="bi bi-{{ $icon ?? 'layers' }}"></i>
    </div>
    <div class="box-info-captions">
        <h2>{{ $title ?? 'Total Aset' }}</h2>
        <span class="counter">{{ $counter ?? '0' }}</span>
    </div>
</div>
